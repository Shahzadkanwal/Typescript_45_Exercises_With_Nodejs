//define a function to each megician name from an array 

function show_megicians(megicians:string []){
    megicians.forEach(name => console.log(name))
        
    };
//define the array containing megician names

    let megicians_names = ["Abid","Kashif","usama"];

//call the functions name to print each megicians name

show_megicians(megicians_names);
